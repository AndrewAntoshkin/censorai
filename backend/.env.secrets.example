# Copy to .env.secrets (gitignored). Values = Vercel → Project → Environment Variables → Production.
REPLICATE_API_TOKEN=r8_xxxxxxxx
REPLICATE_MODEL=google/gemini-3.5-flash
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxx
